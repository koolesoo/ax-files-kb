# Локальная разработка — скопируйте в .env
OPENAI_API_KEY=sk-your-key-here

# Опционально (есть разумные значения по умолчанию в коде)
# OPENAI_MODEL=
# OPENAI_EMBEDDING_MODEL=
# RAG_SCORE_THRESHOLD=
# RAG_ASK_MIN_SCORE=

# Netlify: достаточно OPENAI_API_KEY в Environment variables.
# Остальные переменные добавляйте только если нужно переопределить дефолты.
